let code =
  "without giving data type to variables is knows as type- inferencing";
code = "inference";
// code = false
// is not allowed

let project: boolean = true;
// we have to tell them what type of data it will store its known as type- anotations
project = false;

// Type annotation is where you manually declare a variable's type, while type inference is when TypeScript automatically figures out the type for you